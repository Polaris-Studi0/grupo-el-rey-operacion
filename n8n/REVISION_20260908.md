# Revisión comercial de WhatsApp — 8 de septiembre de 2026

## Reinicio de pruebas y búsqueda de productos — última actualización 9 de septiembre

Publicado 03 `b5b5af00-803e-43ae-b3fd-8ebee75c389f` y aplicado `202609090001_whatsapp_purchase_reset.sql`. La función instalada coincide con la probada: MD5 `2af21480a3a491bdb4ab8840d6b08e79`.

- Un saludo simple responde a ese saludo sin crear verificaciones del pago anterior.
- «Ya no quiero eso» descarta la selección pendiente, cancela sus tareas y conserva consentimiento/sede; una cancelación de pedido ya registrado requiere revisión humana. El contexto deja de usar mensajes y tareas anteriores al reinicio.
- `product_interest` conserva qué se está buscando; «porfa» y «cuáles tienes» continúan ese interés. Sin catálogo confirmado se pide la sede y se crea una consulta de producto real. No se exige dirección/destinatario antes de elegir.
- El contrato exige `action` y `reply`; los campos técnicos opcionales omitidos se conservan/defaultan en el normalizador. La ejecución 1209 falló al omitir `payment_reference`, aunque el resto del JSON era correcto. No se relajaron las verificaciones de pago del normalizador o PostgreSQL.
- Se quitó `builtInTools: {}` del modelo porque esa opción vacía era incompatible con su configuración actual. Se conservó el modelo/credencial que ya funcionaban. Validación de n8n sin advertencias.

Validación local: 68 pruebas de normalización/entrada/contrato y 16 escenarios PostgreSQL; ESLint y comprobación del diff. Incluye limpieza que aborta ante contactos ajenos, pedidos o movimientos/reservas de stock, y guarda respaldo antes de vaciar. No se generaron nuevas conversaciones ni mensajes de WhatsApp ni se ejecutó nuevamente el modelo para estas pruebas.

Limpieza autorizada: se comprobó que solo había el contacto de pruebas de Samuel, dos conversaciones y 60 mensajes, sin pedidos/adjuntos. Se respaldaron 506 filas relacionadas en el esquema privado `elrey_test_backups`, tabla `whatsapp_20260909`, con permisos revocados para los roles API y RLS habilitado desde el editor. Después se verificaron cero contactos, conversaciones, mensajes y pedidos. Catálogo, sedes, usuarios, QR y configuración se conservaron. El historial técnico de ejecuciones de n8n es independiente y permanece disponible.

El script de mantenimiento está en `scripts/reset-whatsapp-test-history.sql`: es de una sola aplicación y falla si el respaldo ya existe. Las filas anteriores pueden recuperarse por un administrador desde la tabla privada (columna `table_name` identifica la tabla original y `row_data` contiene la fila completa); no exponer ese respaldo mediante la API. El consecutivo no se modificó. El horario continúa abierto temporalmente para pruebas. Queda comprobar la nueva experiencia real por WhatsApp y cargar ofertas cuando se definan.

## Corrección tras transcripción real — 9 de septiembre

Se revisaron las ejecuciones reales 1167–1197 sin volver a ejecutarlas. Los mensajes de «solicité cotización» y «pedí agendar» tenían `action=reply`/`ask_branch`: no crearon las tareas anunciadas. `items_verified` seguía falso porque la consulta inicial no tenía carrito y el responsable describió un producto más específico. La tarifa anunciada por el modelo seguía nula en el estado. No había pedido registrado en el último contexto analizado y el pago permanecía pendiente.

Cambios publicados en 03:

- Recuperar destinatario de respuestas combinadas como «Samuel, confirmo dirección» sin volver a preguntarlo.
- Reconocer productos descritos por una resolución humana anterior al carrito, comprobando variante, precio exacto y cantidad explícita. Cambios de color, precio o cantidad no adquieren esa aprobación.
- Crear la tarea real de domicilio al reunir los datos; mantener título y pregunta coherentes si primero hace falta verificar productos.
- Aceptar una tarifa explícita de una instrucción auténtica del operador y evitar volver a pedir la misma cotización.
- Verificar el pago declarado y separar compras previas en tienda: Operación revisa caja y coordina entrega, sin nuevo cobro ni creación automática duplicada. El texto del cliente «OPERACIONES INTERNAS» no confirma pagos.
- Sustituir promesas de programación sin pedido por el paso pendiente; generar el resumen desde el estado validado para no propagar afirmaciones falsas del modelo.
- Permitir campos adicionales en `sales_state` del contrato para tolerar los indicadores que el modelo copiaba del contexto. Se siguen ignorando aprobaciones derivadas propuestas por el modelo; el resto del contrato mantiene sus requisitos. La ejecución 1185 falló por esos campos adicionales.

Validación: 58 pruebas locales de mensajes/entrada (incluidas 15 regresiones de esta transcripción), 14 escenarios de PostgreSQL y ESLint. Se reprodujeron localmente respuestas reales de seis turnos. No se enviaron mensajes nuevos, no se aprobaron pagos ni se crearon pedidos de prueba. La calidad de nuevas respuestas del modelo aún debe comprobarse por WhatsApp.

Los avisos internos de la transcripción se explican porque el destinatario administrativo configurado en 04 coincide con el número usado por Samuel para probar; no se cambió esa configuración. El horario continúa temporalmente abierto para pruebas. El nodo de modelo y su configuración actual se conservaron; n8n reportó un aviso preexistente de `builtInTools` con Responses API desactivada.

## Resultado aplicado

Tercera incidencia real: la credencial gratuita rechazaba `gpt-5.4-mini` por modelo no permitido. Se consultó `searchModels` con la credencial real y devolvió `gpt-5-mini`, `gpt-5-nano` y `gpt-5.6-luna`. Se seleccionó y publicó `gpt-5-mini` y se renombró el nodo a `GPT-5 Mini · bajo costo`. No se sustituyó la credencial ni se contrató ningún servicio. Falta confirmar ejecución real satisfactoria; una publicación exitosa no prueba la inferencia.

Segunda incidencia real: `Extra Body` del modelo exige una cadena JSON; el objeto `{store:false}` se convertía en `[object Object]`. Se corrigió a la cadena `{"store":false}` tanto en n8n como en el generador local. El mensaje repetido de pendiente era consecuencia del respaldo al fallar el modelo. La conectividad y respuesta real siguen pendientes de confirmar después de este ajuste.

Incidencia posterior en prueba real (21:22): al responder «Acepto», el modelo falló antes de llamar a OpenAI con `Gateway credits eligibility check failed: nodeNotCovered`. La ejecución general figuraba como success por la respuesta de respaldo. Se vinculó explícitamente la credencial existente `wnBGAjYX8OzatHnU` en el nodo del modelo y se publicó 03 versión `e662dbee-0298-4207-9463-d023ba855ad5`. Pendiente confirmar respuesta real posterior a esta corrección; no confundir las pruebas locales con conectividad del modelo.

Se revisaron los cinco flujos existentes. Se publicaron cambios en 01 (entrada), 03 (asistente), 04 (pendientes humanos) y 05 (zona horaria Colombia). El consentimiento de 02 se conserva.

Worker desplegado y confirmado por Cloudflare: versión `2ed49e68-71b3-4ab3-8217-9710a1a13b89`. Copias previas conservadas también en `/Users/samuel/Documents/ChatGPT/Grupo El Rey/respaldos-n8n-20260908/`, fuera del directorio temporal.

- Ofertas de temporada como prioridad, respuestas útiles sin exigir nombre o sede para cada pregunta y sin inventar descuentos. El usuario confirmó que las ofertas aún no están definidas; no se cargaron productos ni promociones ficticias.
- Estado de compra más consistente: correcciones de productos, cantidades, sede, dirección por fragmentos, domicilio y medio de pago. Los cambios materiales invalidan cotizaciones y aprobaciones anteriores.
- Confirmación del pedido y aprobación humana de pagos verificadas también en PostgreSQL. «No aprobado» no cuenta como aprobación. Protección frente a duplicados y falta de inventario al confirmar.
- QR únicamente cuando corresponde y posibilidad de volver a pedirlo; cada solicitud conserva su idempotencia.
- Respeto de toma manual, descarte de respuestas anteriores cuando llega un mensaje más reciente y actualización del contexto de pendientes humanos.
- Reutilización de respuestas ya calculadas durante reintentos, recuperación de mensajes persistidos sin completar y asociación correcta de contactos en lotes.
- La notificación de pendientes desde 03 llama a 04 como subflujo interno. Se limita la repetición de llamadas al modelo y se reduce el contexto redundante. El límite de salida sube a 1800 tokens para evitar JSON incompleto.
- Corrección del Worker para procesar lotes de Meta que contienen estados de entrega y mensajes simultáneamente.

## Coste y límites

Dos entradas visibles en el historial de n8n pueden corresponder a un flujo principal y su subflujo. Según la documentación oficial, los subflujos no cuentan como ejecuciones adicionales del plan: https://github.com/n8n-io/n8n-docs/blob/main/docs/build/flow-logic/break-workflows-into-smaller-parts.md . Esto no elimina el coste del modelo ni ejecuciones independientes por eventos humanos o el horario programado. No se midió una factura real posterior al cambio.

La recepción reconoce selecciones de sede mediante `list_reply.id = branch:<id>`. El envío de listas y botones de WhatsApp sigue pendiente: no se implementó la interfaz interactiva saliente.

## Verificación

- 43 pruebas locales de normalización comercial y entrada de WhatsApp aprobadas.
- 14 escenarios de integración en PostgreSQL local con PGlite y las migraciones reales: caché, pendientes, cambio de sede, control manual, cierre, mensajes posteriores, QR, pedidos, inventario, aprobación humana y reintentos.
- `npm run check` y `npm run build` aprobados. Sigue el aviso previo de tamaño del bundle.
- Migración `202609080001_whatsapp_commercial_v3.sql` aplicada en Supabase. Hash de la función coincide con el probado: `7344a97ae67b1afbe1b29a307b648c5e`. Al verificar seguían cero conversaciones y cero pedidos.
- Parámetros de los borradores contrastados con los cambios preparados antes de publicar. Publicación confirmada por n8n.
- No se enviaron mensajes reales ni se dispararon conversaciones de prueba. Falta validar el comportamiento del modelo y todo el recorrido por WhatsApp con una conversación real.

## Fuentes y continuidad

Las fuentes editables están en `n8n/commercial/` y `n8n/inbound/`. `npm run test:whatsapp` ejecuta la suite. Las copias previas de los cinco flujos están en `/tmp/elrey-n8n-review-20260908/workflow-N-before.json`; son respaldo temporal y deben conservarse fuera de `/tmp` si se necesitan a largo plazo. El generador `scripts/build-n8n-commercial-updates.mjs` requiere ese directorio de copias previas y solo produce operaciones revisables: no conecta ni publica y no debe aplicarse dos veces sobre el mismo grafo.

Versiones publicadas: 01 `b62c9170-ad48-4ebe-88e7-e1da03215350`; 03 `6345a89f-c9ec-4e59-ace1-41c4a1cbab34`; 04 `5d624e2d-aecc-4aeb-a831-210fc2818091`; 05 `474ce59c-1446-452d-abc4-46365995f5d4`.

Se mantiene temporalmente `service_open = true` y `delivery_open = true`, como estaban para pruebas. Restaurar atención 09:00–20:00 y domicilios 09:00–19:00 antes de abrir a clientes. La política nueva queda en `ai_runs.output.policy_version = commercial-v3`; el campo heredado `prompt_version` conserva el valor de la función anterior.

Siguiente sesión: cargar ofertas cuando se definan, probar compra completa por WhatsApp y luego preparar listas de sedes. Los cambios locales quedan sin commit ni push para GitHub Desktop.

Última actualización de cierre: 03 publicado `54b1bd96-931f-4031-ae13-5c1b2b227304`; migración `202609090002_whatsapp_payment_checkout.sql` aplicada (hash `1f7a173191c12073da25e3a431878e58`). QR en primera elección, `Validado` con evidencia humana, `Todo correcto` activa creación efectiva, confirmación numerada solo tras insertar. 87 pruebas unitarias y 17 escenarios PostgreSQL pasan, con pedido de producto manual + domicilio + reintento sin duplicar. No se reejecutaron mensajes ni se creó el pedido de prueba en producción; continuar desde el chat actual con `Confirmo el pedido`.

Actualización 2026-09-09 tarde: última versión activa de 03 `e40f12af-6b74-4d0c-ad49-a74fd3cca2a9`, igual al borrador. Corregida evidencia abreviada de producto vinculada a la consulta original, avance sin reconfirmar stock y barrio explícito de destino. Reproducciones locales de ejecuciones 1225/1229/1235 pasan; 79 pruebas unitarias + 16 escenarios de base de datos y ESLint pasan. Sin envío real de WhatsApp ni limpieza adicional de datos. Ver detalles en HANDOFF_N8N_WHATSAPP.md.
